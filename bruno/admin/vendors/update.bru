meta {
  name: update
  type: http
  seq: 4
}

post {
  url: {{base_url}}/admin/vendors/:id
  body: json
  auth: inherit
}

params:path {
  id: {{admin_vendor_id}}
}

body:json {
  {
    "name": "Acme Updated"
  }
}
